# ── Frontend Environment ──────────────────────────────────────────
# Minimal env for frontend container — API_BASE_URL only.
# No secrets here; frontend never touches backend API keys or DB.

# URL of the backend API service (update for your deployment)
API_BASE_URL=http://api:8000
